import { setAccount, setAddress } from '../store/slicers/data';
import { useProvider } from './useProvider';
import { batch } from "react-redux"

export const useDisconnect = (dispatch) => {
 
    
};